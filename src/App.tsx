/**
 * The application, which is a placeholder until `frontend-shell` gives it a
 * router, a session and a layout.
 *
 * It exists so the toolchain is proven end to end — the build, the type check,
 * the lint and the tests all run against something real — rather than against
 * a template's demo counter.
 */
export function App() {
  return (
    <main>
      <h1>CubeForge</h1>
      <p>Multi-tenant analytics. The dashboard starts here.</p>
    </main>
  );
}
